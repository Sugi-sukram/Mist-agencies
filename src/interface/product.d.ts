export interface IProduct {
    image: string; // Assuming `productPh` is a string, otherwise adjust accordingly
    title: string;
    description: string;
    quantity: number;
    volume: number;
    unit: string;
    originalPrice: number;
    discountPrice: number;
  }
  
