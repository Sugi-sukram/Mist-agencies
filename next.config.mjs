/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "mistagencies.com",
      "drive.google.com",
      "doted-dev.s3.ap-southeast-1.amazonaws.com",
    ],
  },
};

export default nextConfig;
